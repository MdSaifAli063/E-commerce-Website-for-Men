<!DOCTYPE html>
<html lang="en" style="height:100%">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= categoryName %> - Elegant Fashion
    </title>
    <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;700&family=Inter:wght@400;700&family=Poppins:wght@400;700&display=swap"
        rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Inter', Arial, sans-serif;
            background: linear-gradient(135deg, #f7f7f8 0%, #f4e4a6 100%);
            color: #1a2332;
        }

        .container {
            max-width: 1600px;
            margin: 0 auto;
            padding: 0 32px;
        }

        header {
            background: linear-gradient(90deg, #fff 80%, #f4e4a6 100%);
            box-shadow: 0 10px 30px rgba(26, 35, 50, 0.15);
            position: sticky;
            top: 0;
            z-index: 50;
            border-bottom: 1px solid #e8e4e0;
            border-radius: 0 0 24px 24px;
            backdrop-filter: blur(10px);
        }

        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 70px;
            position: relative;
            padding: 0 15px;
            max-width: 1400px;
            margin: 0 auto;
        }

        nav {
            position: absolute;
            left: 45%;
            transform: translateX(-50%);
            display: flex;
            gap: 8px;
            flex-shrink: 0;
            z-index: 10;
        }

        nav button {
            background: none;
            border: none;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 500;
            font-size: 13px;
            color: #1a2332;
            cursor: pointer;
            transition: color 0.2s, transform 0.2s, background 0.2s;
            padding: 8px 16px;
            border-radius: 8px 16px 16px 8px;
            position: relative;
        }

        nav button.active,
        nav button:hover {
            color: #d4af37;
            background: none;
            box-shadow: none;
            position: relative;
        }

        nav button.active::after,
        nav button:hover::after {
            content: "";
            display: block;
            position: absolute;
            left: 16px;
            right: 16px;
            bottom: 4px;
            height: 3px;
            border-radius: 2px;
            background: linear-gradient(90deg, #d4af37 60%, #f4e4a6 100%);
            transition: width 0.2s;
        }

        nav button:not(:first-child) {
            color: #1a2332;
        }

        .site-title {
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: bold;
            font-size: 28px;
            color: #1a2332;
            background: none;
            border: none;
            cursor: pointer;
            transition: color 0.2s, text-shadow 0.2s;
            letter-spacing: 1.5px;
            text-shadow: 0 2px 8px #f4e4a6;
            margin: 0;
        }

        .site-title:hover {
            color: #d4af37;
            text-shadow: 0 4px 16px #d4af37;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 12px;
            flex-shrink: 0;
        }

        .header-actions button.icon {
            background: none;
            border: none;
            cursor: pointer;
            color: #1a2332;
            font-size: 18px;
            transition: all 0.3s ease;
            border-radius: 50%;
            padding: 8px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 4px;
            position: relative;
            width: 40px;
            height: 40px;
        }

        .header-actions button.icon:hover {
            color: #d4af37;
            background: linear-gradient(135deg, #f4e4a6 0%, #d4af37 100%);
            transform: scale(1.12);
            box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
        }

        .btn-primary,
        .btn-secondary,
        .btn-gold {
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
            color: #1a2332;
            border: none;
            border-radius: 8px;
            padding: 12px 32px;
            font-size: 16px;
            cursor: pointer;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 600;
            box-shadow: 0 2px 8px #d4af37;
            transition: background 0.2s, transform 0.2s;
        }

        .btn-primary:hover {
            background: linear-gradient(90deg, #1a2332 80%, #d4af37 100%);
            color: #fff;
            transform: scale(1.07);
        }

        .btn-secondary {
            background: none;
            color: #1a2332;
            border: 2px solid #d4af37;
        }

        .btn-secondary:hover {
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
            color: #1a2332;
            transform: scale(1.07);
        }

        .btn-gold {
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
        }

        .btn-gold:hover {
            background: linear-gradient(90deg, #1a2332 80%, #d4af37 100%);
            color: #fff;
            transform: scale(1.07);
        }

        main {
            padding-bottom: 60px;
        }

        .hero-section {
            min-height: 60vh;
            background: linear-gradient(135deg, rgba(26, 35, 50, 0.75) 0%, rgba(45, 55, 72, 0.75) 60%, rgba(244, 228, 166, 0.75) 100%),
                url('https://images.pexels.com/photos/3874589/pexels-photo-3874589.jpeg?auto=compress&cs=tinysrgb&w=1200') center/cover;
            display: flex;
            align-items: center;
            position: relative;
            border-radius: 0 0 48px 48px;
            box-shadow: 0 8px 32px rgba(212, 175, 55, 0.08);
        }

        .hero-overlay {
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.15);
            border-radius: 0 0 48px 48px;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
            padding: 40px 24px;
        }

        .hero-title {
            font-size: 48px;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: bold;
            color: #fff;
            margin-bottom: 24px;
            line-height: 1.1;
            letter-spacing: 2px;
        }

        .hero-title span {
            display: block;
            background: linear-gradient(90deg, #d4af37, #f4e4a6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            color: transparent;
            font-size: 56px;
            font-weight: 900;
            letter-spacing: 4px;
        }

        .hero-desc {
            font-size: 18px;
            color: #fff;
            opacity: 0.95;
            margin-bottom: 24px;
            font-family: 'Inter', Arial, sans-serif;
            text-shadow: 0 2px 8px #1a2332;
        }

        .section {
            padding: 80px 0;
            background: #fff;
            border-radius: 32px;
            margin: 40px 0;
            box-shadow: 0 4px 24px rgba(212, 175, 55, 0.08);
        }

        .section-title {
            font-size: 40px;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: bold;
            color: #1a2332;
            margin-bottom: 24px;
            letter-spacing: 2px;
        }

        .section-desc {
            font-size: 18px;
            color: #4a5568;
            margin-bottom: 40px;
        }

        .grid {
            display: grid;
            gap: 40px;
        }

        @media (min-width: 900px) {
            .grid {
                grid-template-columns: repeat(4, 1fr);
            }
        }

        @media (max-width: 899px) {
            .grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        .card {
            background: linear-gradient(135deg, #fff 80%, #f4e4a6 100%);
            border-radius: 24px;
            box-shadow: 0 4px 24px rgba(212, 175, 55, 0.12);
            overflow: hidden;
            transition: box-shadow 0.2s, transform 0.2s, border 0.2s;
            cursor: pointer;
            border: 2px solid #f4e4a6;
            position: relative;
        }

        .card:hover {
            box-shadow: 0 8px 32px #d4af37;
            transform: translateY(-8px) scale(1.03);
            border-color: #d4af37;
        }

        .card-img {
            width: 100%;
            height: 256px;
            object-fit: cover;
            border-radius: 24px 24px 0 0;
            box-shadow: 0 2px 8px #d4af37;
            transition: transform 0.2s;
        }

        .card:hover .card-img {
            transform: scale(1.05);
        }

        .card-body {
            padding: 32px 24px 24px 24px;
        }

        .card-label {
            display: inline-block;
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
            color: #1a2332;
            font-weight: 700;
            font-size: 15px;
            border-radius: 10px;
            padding: 6px 20px;
            margin-bottom: 12px;
            box-shadow: 0 2px 8px #d4af37;
        }

        .card-title {
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 700;
            font-size: 24px;
            color: #1a2332;
            margin-bottom: 12px;
            letter-spacing: 1px;
        }

        .card-desc {
            color: #4a5568;
            font-size: 16px;
            margin-bottom: 18px;
        }

        .card-price {
            font-size: 20px;
            color: #d4af37;
            font-weight: 600;
        }

        .card-actions {
            display: flex;
            gap: 12px;
            margin-top: 16px;
        }

        .card-action-btn {
            flex: 1;
            padding: 10px;
            border: 2px solid #d4af37;
            border-radius: 8px;
            background: none;
            color: #1a2332;
            cursor: pointer;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 600;
            transition: background 0.2s, color 0.2s;
        }

        .card-action-btn:hover {
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
            color: #1a2332;
        }

        .card-action-btn.add-to-cart {
            background: linear-gradient(90deg, #d4af37 80%, #f4e4a6 100%);
            color: #1a2332;
            border: none;
        }

        .card-action-btn.add-to-cart:hover {
            background: linear-gradient(90deg, #1a2332 80%, #d4af37 100%);
            color: #fff;
        }

        footer {
            background: linear-gradient(90deg, #1a2332 80%, #d4af37 100%);
            color: #fff;
            padding: 64px 0 32px 0;
            border-radius: 32px 32px 0 0;
            box-shadow: 0 -4px 24px #d4af37;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 40px;
            margin-bottom: 32px;
        }

        .footer-title {
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: bold;
            font-size: 16px;
            margin-bottom: 18px;
            letter-spacing: 1px;
            color: #f4e4a6;
            text-shadow: 0 2px 8px #1a2332;
        }

        .footer-bottom {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 18px;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            padding-top: 18px;
        }

        .footer-bottom button {
            background: none;
            border: none;
            color: rgba(255, 255, 255, 0.7);
            font-size: 16px;
            cursor: pointer;
            margin: 0 8px;
            transition: color 0.2s;
        }

        .footer-bottom button:hover {
            color: #d4af37;
        }

        @media (max-width: 900px) {
            .footer-content {
                grid-template-columns: 1fr;
            }

            .hero-title {
                font-size: 36px;
            }

            .hero-title span {
                font-size: 44px;
            }
        }

        .top-marquee {
            background: #7a0000;
            color: #fff;
            padding: 6px 0;
            text-align: center;
            overflow: hidden;
            font-family: 'Poppins', Arial, sans-serif;
            font-weight: 600;
            font-size: 14px;
            letter-spacing: 0.6px;
        }

        .top-marquee .marquee-content {
            display: inline-block;
            white-space: nowrap;
            padding-left: 100%;
            animation: marquee 14s linear infinite;
        }

        @keyframes marquee {
            0% {
                transform: translateX(0%);
            }

            100% {
                transform: translateX(-100%);
            }
        }
    </style>
</head>

<body>
    <div class="top-marquee" role="region" aria-label="Site announcement">
        <div class="marquee-content">Free delivery on orders over $50 — limited time offer!</div>
    </div>

    <header>
        <div class="container header-content">
            <button class="site-title" onclick="location.href='/'">Elegant Fashion</button>
            <nav>
                <button onclick="location.href='/'">Home</button>
                <button onclick="location.href='/products'">Products</button>
                <button onclick="location.href='/services'">Services</button>
                <button onclick="location.href='/about'">About</button>
                <button onclick="location.href='/contact'">Contact</button>
            </nav>
            <div class="header-actions">
                <form class="search-form">
                    <input type="text" placeholder="Search products...">
                    <button type="submit">
                        <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="11" cy="11" r="8"></circle>
                            <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                        </svg>
                    </button>
                </form>
                <button class="icon" title="Cart">
                    <svg width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"></path>
                        <path d="M3 6h18"></path>
                        <path d="M16 10a4 4 0 0 1-8 0"></path>
                    </svg>
                </button>
            </div>
        </div>
    </header>

    <main>
        <section class="hero-section"
            style="background: linear-gradient(90deg, #0f172a 0%, #1a2332 100%); padding: 100px 32px 60px 32px; position: relative; overflow: visible; width: 100vw; margin-left: calc(-50vw + 50%); min-height: 60vh; display: flex; align-items: center; margin-top: 0; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2); border-radius: 0;">
            <div style="position: absolute; inset: 0; background: rgba(0, 0, 0, 0.2); top: 0;"></div>
            <div class="container"
                style="position: relative; z-index: 1; text-align: center; width: 100%; max-width: 1200px; margin: 0 auto;">
                <div style="margin-bottom: 40px;">
                    <h1
                        style="font-size: 70px; font-family: 'Poppins', Arial, sans-serif; font-weight: 900; color: #d4af37; margin: 0 0 24px 0; line-height: 1.1; letter-spacing: 3px; text-shadow: 0 6px 20px rgba(0,0,0,0.4);">
                        <%= categoryName %> Collection
                    </h1>
                    <p
                        style="font-size: 18px; color: #cbd5e1; margin: 0 0 40px 0; max-width: 800px; margin-left: auto; margin-right: auto; line-height: 1.8;">
                        Explore our premium <%= categoryName %> pieces — crafted with elegance and style.</p>
                </div>
                <div style="display:flex; gap:20px; justify-content:center; flex-wrap:wrap; margin-bottom:40px;">
                    <button class="btn-gold" style="padding:14px 40px; font-size:15px;">Shop Now</button>
                    <button class="btn-secondary" style="padding:14px 40px; font-size:15px;">Explore</button>
                </div>
                <div style="display:flex; justify-content:center; gap:50px; flex-wrap:wrap;">
                    <div style="color:#fff; text-align:center;">
                        <div style="font-size:36px; font-weight:900; color:#d4af37; margin-bottom:8px;">100+</div>
                        <div style="font-size:14px; color:#cbd5e1;">Items</div>
                    </div>
                    <div style="color:#fff; text-align:center;">
                        <div style="font-size:36px; font-weight:900; color:#d4af37; margin-bottom:8px;">Top</div>
                        <div style="font-size:14px; color:#cbd5e1;">Quality Picks</div>
                    </div>
                    <div style="color:#fff; text-align:center;">
                        <div style="font-size:36px; font-weight:900; color:#d4af37; margin-bottom:8px;">⭐</div>
                        <div style="font-size:14px; color:#cbd5e1;">Customer Favorites</div>
                    </div>
                </div>
            </div>
        </section>

        <section class="section">
            <div class="container">
                <div class="section-title">
                    <%= categoryName %>
                </div>
                <div class="section-desc">Handpicked pieces from our <%= categoryName %> collection</div>
                <div class="grid">
                    <% products.forEach(product=> { %>
                        <div class="card">
                            <img class="card-img" src="<%= product.image %>" alt="<%= product.name %>">
                            <div class="card-body">
                                <div class="card-label">
                                    <%= product.label %>
                                </div>
                                <div class="card-title">
                                    <%= product.name %>
                                </div>
                                <div class="card-desc">
                                    <%= product.desc %>
                                </div>
                                <div class="card-price">$<%= product.price %>
                                </div>
                                <div class="card-actions">
                                    <button class="card-action-btn add-to-cart">Add to
                                        Cart</button>
                                    <button class="card-action-btn">❤</button>
                                </div>
                            </div>
                        </div>
                        <% }); %>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <div class="footer-content">
                <div>
                    <div class="footer-title">Elegant Fashion</div>
                    <p>Crafting timeless elegance through premium fashion.</p>
                </div>
                <div>
                    <div class="footer-title">Quick Links</div>
                    <ul style="list-style:none; padding:0;">
                        <li><a href="/" style="color:#fff; text-decoration:none;">Home</a></li>
                        <li><a href="/products" style="color:#fff; text-decoration:none;">Products</a></li>
                    </ul>
                </div>
                <div>
                    <div class="footer-title">Categories</div>
                    <ul style="list-style:none; padding:0;">
                        <li><a href="#" style="color:#fff; text-decoration:none;">
                                <%= categoryName %>
                            </a></li>
                    </ul>
                </div>
                <div>
                    <div class="footer-title">Support</div>
                    <ul style="list-style:none; padding:0;">
                        <li><a href="/contact" style="color:#fff; text-decoration:none;">Contact</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <div>© 2024 Elegant Fashion. All rights reserved.</div>
            </div>
        </div>
    </footer>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const navButtons = document.querySelectorAll("nav button");
            navButtons.forEach((btn) => {
                btn.addEventListener("click", () => {
                    navButtons.forEach((b) => b.classList.remove("active"));
                    btn.classList.add("active");
                });
            });
        });
    </script>
</body>

</html>